{"code":"function decomposeIntoFactorials ($input) {\n    $timesFactored = 0;\n    $reducedValue = intval($input);\n    $factorials = [];\n    while ($reducedValue > 0) {\n        for ($x = 1; $x <= $reducedValue; $x++) {\n            if (factorate($x) > $reducedValue) {\n                $timesFactored++;\n                $factorials[] = ($x-1).'!';\n                $reducedValue = $reducedValue - factorate($x-1);\n                break;\n            } else if (factorate($x) == $reducedValue) {\n                $timesFactored++;\n                $factorials[] = $x.'!';\n                $reducedValue = $reducedValue - factorate($x);\n                break;\n            }\n        }\n    }\n    echo '<b>Input number: <\/b>'.$input;\n    echo '<br \/><br \/>';\n    echo '<b>Number of factorials: <\/b>'.$timesFactored;\n    echo '<br \/><br \/>';\n    echo '<b>Factorial decomposition: <\/b>'.join(' + ', $factorials);\n}\n\nfunction factorate ($inVal, $calc = 1) {\n    if ($inVal - 1 == 0) {\n        return $calc;\n    } else {\n        $calc = $calc*$inVal;\n        $inVal = $inVal - 1;\n        return factorate($inVal, $calc);\n    }\n}"}